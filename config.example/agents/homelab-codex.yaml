# Example: homelab agent using Codex CLI via ACP backend
name: homelab-codex
description: Home automation agent using Codex CLI
enabled: true
models:
  preferred: codex-mini
  complexity: medium
metadata:
  acp_agent: codex
  acp_permissions: approve-reads
tools:
  builtin:
    - Read
    - Write
    - Bash
  confirm_gated:
    - Write
    - Bash
