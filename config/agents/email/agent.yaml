name: email
description: >
  Gmail and Yahoo inbox triage, email search, labeling, archiving,
  cleanup, composing replies, and inbox zero maintenance
enabled: true
models:
  preferred: null
  fallback: null
  auto: true
  complexity: medium
tools:
  builtin:
    - Bash
    - Read
  modules:
    email:
      enabled: true
  confirm_gated:
    - email.send
    - email.archive
memory:
  own_domain: email
  readable_domains: null
  can_read_shared: true
  can_write: true
